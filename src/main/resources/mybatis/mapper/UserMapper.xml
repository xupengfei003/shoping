<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sao.so.supplier.dao.UserDao">
    <resultMap id="BaseResultMap" type="com.sao.so.supplier.domain.User">
       <!-- <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="money" jdbcType="DECIMAL" property="money"/>
        <result column="create_date" jdbcType="TIMESTAMP" property="createDate"/>
        <result column="is_deleted" jdbcType="TINYINT" property="isDeleted"/>-->
        <id column="user_id" jdbcType="BIGINT" property="id"/>
        <result column="login_name" jdbcType="VARCHAR" property="username"/>
        <result column="login_password" jdbcType="VARCHAR" property="password"/>
        <result column="last_password_reset_date" jdbcType="BIGINT" property="lastPasswordResetDate"/>
    </resultMap>
    <select id="findOne" resultMap="BaseResultMap">
        SELECT user_id, login_name, login_password
          FROM t_user
         WHERE user_id=#{id}
    </select>
    <select id="countPage"  resultType="int">
        SELECT CASE WHEN count(*)%${page.pageSize}>0 THEN count(*)/${page.pageSize}+1 ELSE count(*)/${page.pageSize} END
          FROM t_user
    </select>
    <select id="findPage" resultMap="BaseResultMap">
        SELECT user_id, login_name, login_password
          FROM t_user
    </select> 
    <select id="findByUsername" resultMap="BaseResultMap">
        SELECT user_id, login_name, login_password, last_password_reset_date
          FROM t_user
         WHERE login_name=#{username}
    </select>
    <update id="updatePassword">
        UPDATE t_user SET login_password=#{loginPassword} WHERE user_id = #{userId}
    </update>
    <update id="saveSmsCode">
        UPDATE t_user SET sms_code=#{code} WHERE login_name = #{tel}
    </update>
    <select id="findSmsCode" >
        SELECT sms_code FROM t_user WHERE user_id=#{userId}
    </select>
    <insert id="add" parameterType="com.sao.so.supplier.domain.User">
        <selectKey resultType="java.lang.Long" order="AFTER" keyProperty="id">
            SELECT LAST_INSERT_ID() AS id
        </selectKey>
        insert into t_user (login_name, login_password)
        values (#{username,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR})
    </insert>
    <update id="update">
        UPDATE t_user SET login_name=#{loginName} WHERE user_id=#{id}
    </update>
</mapper>